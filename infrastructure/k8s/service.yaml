apiVersion: v1
kind: Service
metadata:
  name: autohub-service
  labels:
    app: autohub
spec:
  type: LoadBalancer
  selector:
    app: autohub
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
